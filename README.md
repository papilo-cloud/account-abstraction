# Account Abstraction (ERC-4337) — Solidity + Foundry

This repository implements Account Abstraction (AA) following the ERC-4337 standard.
It allows smart contract wallets to act as first-class citizens on Ethereum by removing the dependency on externally owned accounts (EOAs).